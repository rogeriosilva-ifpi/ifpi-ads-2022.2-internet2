# Persistence

```bash
$ yarn typeorm migration:generate src/infra/persistence/typeorm/migrations/AddTaskEntity
$ yarn typeorm migration:show
$ yarn typeorm migration:run
$ yarn typeorm migration:revert
```
